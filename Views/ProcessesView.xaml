<UserControl x:Class="SystemMonitorDesktop.Views.ProcessesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent">

    <Grid Margin="30,24,26,30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="Procesos" Style="{StaticResource T.Title}"/>
            <TextBlock Text="Las aplicaciones que más memoria están consumiendo ahora mismo."
                       Style="{StaticResource T.Secondary}" Margin="0,4,0,22"/>
        </StackPanel>

        <Border Grid.Row="1" Style="{StaticResource Card}" Padding="0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="20,16,20,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="80"/>
                        <ColumnDefinition Width="150"/>
                        <ColumnDefinition Width="110"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="Aplicación" Style="{StaticResource T.Label}"/>
                    <TextBlock Grid.Column="1" Text="PID" Style="{StaticResource T.Label}"/>
                    <TextBlock Grid.Column="2" Text="Memoria" Style="{StaticResource T.Label}"/>
                    <TextBlock Grid.Column="3" Text="" Style="{StaticResource T.Label}"/>
                </Grid>

                <Border Grid.Row="1" Style="{StaticResource Divider}"/>

                <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" Padding="20,10,14,14">
                    <StackPanel x:Name="RowsPanel"/>
                </ScrollViewer>
            </Grid>
        </Border>

        <TextBlock Grid.Row="2" x:Name="StatusText" Style="{StaticResource T.Caption}"
                   Margin="2,12,0,0" TextWrapping="Wrap" Text=" "/>
    </Grid>
</UserControl>
